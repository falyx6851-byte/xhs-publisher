name: Unattended Xiaohongshu Publisher

on:
  # 允许手动触发 (输入文章链接)
  workflow_dispatch:
    inputs:
      article_url:
        description: '公众号文章链接 (必填)'
        required: true
      template:
        description: '封面模板 (可选: tech_card/breath/sticky...)'
        required: false
        default: 'breath'

jobs:
  publish:
    runs-on: ubuntu-latest
    
    # 设置超时，防止卡死耗尽额度
    timeout-minutes: 15

    steps:
    - name: 📥 拉取代码
      uses: actions/checkout@v4

    - name: 🐍 设置 Python 环境
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install playwright
        playwright install chromium --with-deps

    - name: 🚀 运行发布脚本
      env:
        # 从 GitHub Secrets 读取敏感信息
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        XHS_COOKIE: ${{ secrets.XHS_COOKIE }}
      run: |
        # 运行专门的 CLI 脚本
        python github_runner.py "${{ inputs.article_url }}" --template "${{ inputs.template }}"

    - name: 📤 上传生成结果 (可选)
      if: success() || failure()
      uses: actions/upload-artifact@v4
      with:
        name: generation-results
        path: temp_output/
        retention-days: 3
